node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# personal trip-plan exports — never commit real saves
trip/sea-trip-plan.json

# personal trip-plan exports — never commit real saves
static/trip/sea-trip-plan.json

# internal handoff notes — not for the repo
HANDOFF.md
